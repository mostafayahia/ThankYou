<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="EqualActionButtonSeparator">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
    </style>

    <style name="FABStyle">
        <item name="android:layout_width">@dimen/fab_size</item>
        <item name="android:layout_height">@dimen/fab_size</item>
        <item name="android:background">@drawable/oval_ripple</item>
        <item name="android:elevation">6dp</item>
        <item name="android:layout_marginBottom">@dimen/fab_margin_vertical</item>
        <item name="android:layout_marginRight">@dimen/fab_margin_horizontal</item>
        <item name="android:layout_marginLeft">@dimen/fab_margin_horizontal</item>
        <item name="android:stateListAnimator">@animator/fab_raise</item>
    </style>
</resources>
